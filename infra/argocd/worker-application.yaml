apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: scrutis-worker
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Scrutis/Scrutis.git
    targetRevision: main
    path: infra/helm/worker

  destination:
    server: https://kubernetes.default.svc
    namespace: scrutis

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
