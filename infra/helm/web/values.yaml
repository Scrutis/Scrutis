replicaCount: 1
image:
  repository: arcayth/scrutis-web
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

env:
  PORT: 3000

envFrom:
  - secretRef:
      name: better-auth-secret
  - secretRef:
      name: db-secret
  - secretRef:
      name: github-secret
